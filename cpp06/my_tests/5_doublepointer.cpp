

#include <iostream>

int main() {
    // Array of C-style strings (array of pointers to char arrays)
    const char *strings[] = {"Hello", "World", "from", "C++"};

    // Double pointer to char
    // const char **doublePtr = strings;




    // Printing each string using double pointer
    // for (int i = 0; i < 4; ++i) {
    //     std::cout << doublePtr[i] << std::endl;
    // }

    return 0;
}
